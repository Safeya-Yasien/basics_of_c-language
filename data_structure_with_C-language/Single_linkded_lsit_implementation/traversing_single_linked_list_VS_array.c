#include <stdio.h>
#include <stdlib.h>


//traversing the list Vs array


int main()
{
    /*
    this code to count number of nodes in linked list and we will
    traverse all the list to count nodes
    ► so the time complexity is O(n)

        while (ptr != NULL)
        {
            count++;
            ptr = ptr->link;
        }

    this code to print nodes of linked list and we will
    traverse all the list to print their data
    ► so the time complexity is O(n)

        while (ptr != NULL)
        {
            printf("%d ", ptr->data);
            ptr = ptr->link;
        }

    this code to count number of element in array and we will
    only write sizeof
    ► so the time complexity is O(1)

        int arr[] = {45, 98, 3};
        int n = sizeof(arr)/sizeof(int)

    this code to print element of array and we will
    traverse all the array elements
    ► so the time complexity is O(n)

        for (i = 0; i < n ; i++)
            printf("%d", arr[i]);
    */


}

